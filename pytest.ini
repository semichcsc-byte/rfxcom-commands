[pytest]
testpaths = tests
# So `custom_components.rfxcom_commands` imports the same way it does inside
# Home Assistant. Without it collection depends on where pytest is invoked.
pythonpath = .
asyncio_mode = auto
addopts = -q
